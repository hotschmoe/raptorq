# Dependencies

None. Pure Zig.

RFC 6330 requires only GF(256) arithmetic, matrix operations, and a deterministic PRNG -- all implemented from scratch following the specification.

No external Zig packages, no C bindings, no system library dependencies.
